package com.droibit.quickly.packages

class PackageActionPerformer : PackageContract.ActionPerformer {

    override fun performPackageAction(action: PackageContract.Action, packageName: String) {
    }
}